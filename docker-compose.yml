version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: flavor_postgres
    restart: always
    environment:
      POSTGRES_USER: flavor_user
      POSTGRES_PASSWORD: fLaVoR_dB_sEcUrE_pAsS_2026
      POSTGRES_DB: flavor_dev
    ports:
      - "5432:5432"
    volumes:
      - flavor_pgdata:/var/lib/postgresql/data

volumes:
  flavor_pgdata:
    driver: local
